# Left half is peripheral in this split setup
# Right half (with trackball) is the central

# Enable split peripheral role
CONFIG_ZMK_SPLIT=y
CONFIG_ZMK_SPLIT_ROLE_CENTRAL=n

# Increase Bluetooth TX power for better connection
CONFIG_BT_CTLR_TX_PWR_PLUS_8=y
